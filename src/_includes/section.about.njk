{% for item in collections.sections %}
  {% if item.data.type === 'about' %}
    <div id="about" class="w-full min-h-screen flex flex-col justify-center items-center py-12">
      <div class="self-center text-center w-full sm:w-2/3 xl:w-1/2 px-4 sm:px:0">
        <h2 class="tracking-widest text-gray-800  text-center text-5xl sm:text-6xl font-black uppercase mb-4">{{ item.data.title }}</h2>
        <div class="font-light text-gray-600 text-xl">
          {{ item.templateContent | safe }}
        </div>
        <img class="my-12 max-w-full mx-auto" src="/img/undraw_asset_selection_ix3v.svg" alt="AWE.SOME">
      </div>
      <div class="flex flex-row w-full justify-center">
        <a class="px-10 py-2 text-gray-200 bg-orange-400 rounded-full font-mono shadow-md text-lg hover:bg-gray-800 hover:border-red" href="#features">features ↓</a>
      </div>
    </div>
  {% endif %}
{% endfor %}
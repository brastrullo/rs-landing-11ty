{% for item in collections.sections %}
  {% if item.data.type === 'contact' %}
    <div id="contact" class="flex flex-col items-center justify-center w-full min-h-screen bg-gray-800">
      <div class="self-center text-center w-full xl:w-[800px] px-4 sm:px:0">
        <h2 class="mb-4 text-4xl italic font-black tracking-widest text-gray-200 uppercase">{{ item.data.title }}</h2>
        <div class="font-mono text-2xl font-light text-gray-400 sm:text-3xl">
          {{ item.templateContent | safe }}
        </div>
        <div class="my-6">
          <button data-cal-link="rastrstudio/discovery-session" data-cal-config='{"layout":"month_view"}' target="_blank" rel="noopener" class="font-mono text-xl font-black text-orange-400 uppercase hover:text-red-300">Schedule a meeting</button>
        </div>
        {# <form class="w-full max-w-lg mx-auto my-20" action="#" method="post" id="contact-form" target="_blank" validate> #}
          {# <div class="flex items-center py-2 border-b border-red-400">
            <input class="w-full px-3 py-2 mr-3 font-thin bg-transparent border border-transparent appearance-none text-gray-50 focus:outline-none focus:bg-white focus:text-gray-900 focus:border-white" type="email" value="" name="EMAIL" placeholder="lenina.crowne@thehatchery.co" aria-label="Email Address" required>
            <button class="flex-shrink-0 px-4 py-2 font-extrabold text-white bg-orange-300 border border-orange-300 hover:bg-gray-800 hover:text-orange-300" type="submit" name="contact">
              send
            </button>
          </div> #}
          {# <label class="block px-2 mt-4 text-left text-gray-200" for="gdpr_37169">
            <input class="leading-tight" type="checkbox" id="gdpr_37169" name="gdpr[37169]" value="Y" required>
            <span class="font-light text-md">
              I agree with the <a href="/privacy" class="underline hover:no-underline hover:text-orange-400" title="Privacy Policy" target="_blank">Privacy Policy</a>
            </span>
          </label> #}
        {# </form> #}
      </div>
    </div>
  {% endif %}
{% endfor %}